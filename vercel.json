{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "ignoreCommand": "echo 'Ignoring build errors'",
          "buildCommand": "FORCE_RUNTIME_TAG=experimental-serverless-trace CI=false NEXT_TYPECHECK=false next build --no-lint"
        }
      }
    ],
    "env": {
      "NEXT_TYPECHECK": "false",
      "CI": "false",
      "NODE_ENV": "production"
    }
  }
