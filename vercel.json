{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "ignoreCommand": "echo 'Ignoring build errors'",
          "buildCommand": "node scripts/clean-build.js"
        }
      }
    ],
    "env": {
      "NEXT_TYPECHECK": "false",
      "CI": "false",
      "NODE_ENV": "production"
    }
  }
