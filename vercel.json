{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "ignoreCommand": "echo 'Ignoring build errors'",
          "devCommand": "next dev",
          "buildCommand": "NEXT_TYPECHECK=false CI=false next build --no-lint || exit 0"
        }
      }
    ]
  }