{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "ignoreCommand": "echo 'Ignoring build errors'",
          "buildCommand": "next build --no-lint"
        }
      }
    ],
    "env": {
      "NEXT_TYPECHECK": "false",
      "CI": "false",
      "NODE_ENV": "production"
    }
  }
